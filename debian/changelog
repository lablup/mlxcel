mlxcel (0.4.1-1~jammy1) jammy; urgency=medium

  * Added: server tool-call parsers for Kimi K2 (#783), the pythonic
    [func(arg=value)] format (#786), function-calling Gemma start_function_call
    (#791), MiniMax-M3 namespaced XML (#793), and GLM-4.7 / LongCat arg_key /
    arg_value grammar (#794).
  * Added: Step-3 (step3p7) model port (#781), Command MoE (Cohere2 MoE) model
    port (#761), and Kimi-VL 3D MoonViT video support (#762).
  * Added: Gemma 4 per-request image soft-token budget via --image-soft-tokens
    and the server detail / max_soft_tokens fields (#787).
  * Fixed: Gemma 4 E-series audio transcription no longer garbles; the Conformer
    mel front-end and per-layer clamp bounds now match the reference (#796).
  * Fixed: Pixtral and Mistral 3 preserve image aspect ratio with row-structured
    IMG_BREAK / IMG_END tokens and CLIP normalization (#792).
  * Fixed: DiffusionGemma tool-call markers demoted from special tokens so tool
    calls parse (#789).
  * Fixed: Qwen3.5 norm shift gated on the conv1d layout so a bundled MTP head
    cannot double-shift a converted checkpoint (#784).
  * Fixed: bracketed Mistral tool-call parsing (#785), attention-sink prefix
    pinned on dense --max-kv-size trim (#759), and a needless borrow in the
    mlxcel-core build script (#795).
  * CI: publish a CycloneDX SBOM on release (#758); drop the broken log-level
    input from the cargo-deny action (#790).
  * Dependencies: bump the minor-and-patch group with 5 updates (#780).

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 16 Jul 2026 09:00:00 +0000

mlxcel (0.4.0-1~jammy1) jammy; urgency=medium

  * Added: experimental OpenXLA / IREE compiler backend, opt-in and default-off
    behind the xla-backend / xla-iree features, with a Rust-native StableHLO
    emitter, IREE execution on Metal and CUDA, dense/MoE architecture packs, and
    a continuous-batching serving path (#449 and follow-ups).
  * Added: ComputeBackend seam abstracting the forward-execution engine (#338).
  * Added: vision-language families Qwen3-Omni thinker + talker speech output
    (#664, #677), Llama 3.2 Vision cross-attention (#596), GLM-4V and GLM-4V MoE
    (#594, #598), Hunyuan-VL (#663), ERNIE-4.5 MoE VL (#662), DeepSeek-VL2
    (#660), Kimi-VL / 2.5 (#597), FastVLM (#661), Moondream2 (#599), Idefics2
    (#639), SmolVLM (#593), LFM2-VL (#645), Granite Vision and Granite 4 Vision
    (#647, #649).
  * Added: OCR families DeepSeek-OCR and DeepSeek-OCR 2 (#655, #656), dots.ocr
    (#657), GLM-OCR (#646), and PaddleOCR-VL (#595).
  * Added: LLaDA-2 MoE masked-diffusion LLM (#659), DeepSeek-V3.2 / GLM-MoE DSA
    lightning indexer (#583), phi3-small blocksparse attention (#581), and
    qwen3-next pipeline-parallel stages (#580).
  * Changed: server and CLI enable batched decode (--parallel 4), batched
    prefill (--max-batch-prefill 4), and the prompt-prefix cache by default,
    with an auto KV-cache budget guard (#628).
  * Performance: CUDA/GB10 kernel parity: native paged-attention decode (#731),
    fused SSM decode (#727), MoE prefill sorted grouped GEMM (#726), Blackwell
    sm_120/121 qmm tile (#723), single-dtype decode graph (#732), and multirow
    qmv (#740).
  * Performance: tick-cooperative speculative decoding (#745), pipelined
    scheduler decode with lookahead async_eval (#729), and NVFP4 direct
    transcode to native MLX (#697, #706).
  * Performance: cap the batched-prefill transient memory by token budget
    (#715).
  * Fixed: interrupted downloads are detected and re-fetched at load instead of
    failing with a bare "Weight not found" (#465).
  * Fixed: GPT-OSS Harmony tool calls parsed into structured calls (#658), plus
    real-checkpoint loading fixes across moondream2, kimi-vl, paddleocr-vl,
    smolvlm, mllama, glm4-moe, and qwen3-next.
  * Chore: bump MLX pin to 0.32.1 (#704); align member crates to Rust edition
    2024 (#445).

 -- Jeongkyu Shin <inureyes@gmail.com>  Sun, 12 Jul 2026 12:00:00 -0700

mlxcel (0.3.3-1~jammy1) jammy; urgency=medium

  * Added: multi-node disaggregated prefill/decode routing with health
    checks and failover (#388), and /v1/completions on the router (#386).
  * Added: Mellum 2 hybrid-attention MoE text model (#397) and video input
    for Gemma 4 Unified (#400).
  * Added: Phase 1 Python client package over the server (#411).
  * Added: MTP speculative decode wired into offline generate (#385).
  * Performance: fused single-launch xIELU Metal kernel for Apertus, on by
    default after M5 Max validation (#414, #417, #399).
  * Performance: wire MiniMax to the fused decode-MoE kernel (#390); bound
    the audio request queue with a per-request timeout (#381).
  * Fixed: sliding-window prefill beyond the window corrected across models,
    with the gemma3/gemma4 mask hoisted to a shared helper (#405, #412, #415).
  * Fixed: OLMoE scores the MoE router with full softmax then gather (#391).
  * Fixed: preserve the assistant reasoning field across turns (#394).
  * Fixed: router hardening for /router/stats and authoritative usage token
    counts (#392, #393).
  * Fixed: audio synthesis panic-safe in release and audio-path FFI ops made
    fallible (#383, #384).
  * Fixed: accept system-role turns inside the /v1/messages array (#380).
  * Added: N-gram loop detection, default-on for the Gemma 4 family (#433),
    and Nemotron-H Nano Omni audio input in server chat (#443).
  * Fixed: prefill attention masks sized from the live window under
    --max-kv-size trim across dense-cache, mistral4, nemotron_nas, qwen-vl,
    gemma3/gemma4, and exaone_moe (#418, #420, #422, #431).
  * Fixed: double-transpose crash on mlx-community conv checkpoints (#429).
  * Fixed: conv1d/conv2d and nemotron audio-encoder convs fallible at the FFI
    boundary so conv shape faults no longer abort the server (#434, #439).
  * Fixed: Gemma 4 audio placed in the user turn, CLI resamples to 16 kHz
    (#438, #440).
  * Fixed: mistral4 MLA backbone routing and 2D MoE token flattening
    (#423, #425).
  * CI: bump actions/checkout from 6 to 7 (#395).
  * Chore: update dependencies to latest compatible versions (#406).

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 25 Jun 2026 12:18:07 -0700

mlxcel (0.3.2-1~jammy1) jammy; urgency=medium

  * Added: Whisper speech-to-text (#371) and Kokoro-82M text-to-speech with an
    iSTFTNet vocoder (#374) on the OpenAI /v1/audio/* endpoints, with the audio
    request and response plumbing (#368).
  * Added: reasoning_content on non-streaming chat completions (#359).
  * Performance: hardware-gated MLX_MAX_OPS_PER_BUFFER decode default raises
    pre-M5 Apple Silicon decode by about 8-12% (#360).
  * Performance: Turbo4Asym decode rerouted through dequant-then-SDPA, about
    0.14x to 0.40x of fp16 with byte-exact output (#369).
  * Performance: single-dispatch batched sampler (#339), incremental penalty
    caches (#344), batched-prefill cohorts (#346), adaptive B=1 MTP policy
    (#348), and the --recommend-quant Turbo KV advisor (#343).
  * Fixed: f16/bf16 logprobs crash and corruption (#340).
  * Fixed: suppress gemma4_unified multimodal placeholder tokens (#351).
  * Fixed: reseed RNG per row at the batched-prefill first-token sample (#356).

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 20 Jun 2026 19:31:52 +0900

mlxcel (0.3.1-1~jammy1) jammy; urgency=medium

  * Performance: fused decode-MoE kernel ported to CUDA, so Linux/CUDA GPUs
    get the fast single-token MoE decode path with byte-identical output;
    gains of about 10-55%, up to 1.55x on qwen3-moe (#319).
  * Performance: wired six more MoE families to the fused decode-MoE kernel:
    qwen2_moe, LFM2, qwen3_vl_moe, Mixtral, Phi-3.5-MoE, and OLMoE (#308,
    #309, #310, #311, #312, #314). The kernel self-gates by expert size
    (MLXCEL_FUSED_MOE_MAX_DFF, default 4096).
  * Performance: BitNet BitLinear ternary matmul ported to CUDA (#322).
  * Fixed: load non-affine quantized VLM weights with the correct quant mode
    and group size, so checkpoints such as minicpm-v mxfp4 load (#334).
  * Fixed: OLMoE applies q_norm/k_norm before the head reshape (#317).
  * Fixed: report load/run OOM as SKIP:oom, not FAIL:bench (#298).
  * CI: add MLXCEL_CXX_MARCH override and pin the x86_64 CUDA release asset
    to x86-64-v3 (#208).

 -- Jeongkyu Shin <inureyes@gmail.com>  Wed, 17 Jun 2026 12:00:00 +0900

mlxcel (0.3.0-1~jammy1) jammy; urgency=medium

  * Added: nine new model families: BitNet b1.58 (#252), IBM Granite dense
    (#254) and GraniteMoeHybrid (#259), LFM2 and LFM2-MoE (#255), Falcon-H1
    (#256), PLaMo 2 with PlamoTokenizer (#257, #264), Apertus (#260),
    ByteDance Seed-OSS (#261), and dots.llm1 MoE (#263).
  * Added: Linux x86_64 and aarch64 CUDA release builds with bundled CCCL
    headers (#262).
  * Added: configurable allowed-origins for server CORS (#253).
  * Changed: fused decode-MoE Metal kernel is now on by default
    (MLXCEL_FUSED_MOE), about 13% faster MoE decode on gemma4 (#285).
  * Performance: two-kernel fused decode-MoE that beats gather_qmm, extended
    to 6-bit and mixed-bit experts and wired to dots.llm1, qwen3-next, and
    gemma4 (#274, #275, #276, #278, #279, #281).
  * Performance: CCCL header resolution that handles relative invocations and
    other nodes, plus a persistent PTX kernel cache for the CUDA backend (#270).
  * Fixed: quantized models stay bf16, fixing a 33-41% M1 Ultra decode
    regression on bf16-scale checkpoints (#290).
  * Fixed: infer per-tensor quantization bits for embeddings so
    mixed-precision exports such as diffusiongemma load (#292).

 -- Jeongkyu Shin <inureyes@gmail.com>  Mon, 15 Jun 2026 12:00:00 +0900

mlxcel (0.2.1-1~jammy1) jammy; urgency=medium

  * Added: exact-prefix prompt-cache snapshots now cover model-owned
    recurrent and mixed-cache families, including Mamba, Mamba2, Jamba,
    Nemotron-H, and Qwen 3.5 / 3.6 text, MoE, and VLM wrappers (#241).
  * Added: Gemma 4 text, VLM, and Unified wrappers now donate and restore
    exact-prefix prompt-cache snapshots for model-owned standard and rotating
    caches; gemma-4-26b-a4b-it-4bit inserted a 10,568,520-byte snapshot in
    smoke validation with no oversized rejection (#243).
  * Docs: align mlxcel serve and mlxcel-server option descriptions for
    disaggregated peer roles, VLM prefix-cache env settings, paged KV budget
    settings, and Gemma 4 snapshot-cache support.

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 13 Jun 2026 19:45:00 +0900

mlxcel (0.2.0-1~jammy1) jammy; urgency=medium

  * Added: unified paged KV cache is live in the batching server (epic
    #116). Prefix reuse and paged block storage now run together: a shared
    prefix is stored once with reference counting and copy-on-write, so a
    second request adopts the existing blocks and re-prefills only its
    divergent suffix. Pool-backed decode is byte-identical to the dense
    path on qwen3 and llama3 (#152, #167, #168).
  * Added: disaggregated serving. `mlxcel-server --node-role
    {prefill,decode,router}` with --serving-bind, --prefill-peers, and
    --decode-peers splits prefill, decode, and a model-free router across
    processes over TCP; a 3-process run is byte-identical to a single
    hybrid node. KV block contents serialize across the handoff
    (#185, #187, #188, #189, #190, #191, #192, #193).
  * Added: DiffusionGemma block-diffusion model: text generation, image
    input, and mlxcel-server serving. Temperature-0 output is
    byte-identical across the MLX bump (#217, #218, #219, #220).
  * Added: Qwen3-Coder XML tool-call parsing, surfaced as OpenAI tool_calls
    (#206).
  * Added: --kv-cache-budget <BYTES|auto> (env MLXCEL_KV_CACHE_BUDGET) caps
    the paged KV block pool with an admission gate and cold-prefix eviction;
    opt-in, unbounded by default. Paged usage is exposed at
    GET /v1/cache/stats and on /metrics (#174, #175, #176, #178).
  * Added: architecture-aware KV-cache memory estimation for inspect and
    the --estimate-memory preflight, plus an explicit activation term.
    Sliding-window, MLA, hybrid, and pure-SSM models no longer use a flat
    formula that was off by about 100x for Gemma, DeepSeek, and Mamba
    (#172, #173).
  * Added (opt-in): VLM prompt-prefix cache sharing for multi-turn
    same-image conversations behind --enable-vlm-prefix-cache, verified
    byte-identical to a cold prefill on qwen2-vl-2b (#182, #184).
  * Added: fused paged-attention decode Metal kernel (split-K
    flash-decoding), gated off because it does not beat MLX gather-then-SDPA
    at long context on Apple Silicon. Enable with
    MLXCEL_PAGED_ATTENTION_NATIVE (#181).
  * Changed: Automatic Prefix Caching is now enabled by default; the output
    is unchanged (#233).
  * Changed: the prompt-prefix KV cache now serves the Anthropic
    /v1/messages and OpenAI Responses /v1/responses endpoints (#240).
  * Changed: the B=1 MTP speculative-burst default is chosen per hardware.
    Batch-capable targets default on only on M5-class hardware with a neural
    accelerator (M1 Ultra regressed); non-batchable targets stay always-on;
    MLXCEL_ENABLE_MTP_B1 overrides either way (#216).
  * Changed: partially matched paged prefixes are adopted instead of
    declined, and paged adoption clones and pins shared blocks rather than
    consuming them (#230, #232).
  * Changed: vendored MLX bumped to upstream main (2026-06-11); the steel
    GEMM overlay was retired (#223).
  * Perf: chunked slab storage for the paged pool, presized per prefill
    span with eager slab eval, plus per-frame decode token streaming from
    the disaggregated decode role (#237, #229, #214).
  * Fix: per-row position holes broke B>1 batched MTP greedy parity after
    divergent accepts; the surviving K/V is now compacted to each row's
    accepted end with per-row RoPE and a precise mask (#211).
  * Fix: guard empty-batch paged-decode fallbacks and use absolute block
    indexing so a logical_start > 0 write addresses the correct block
    (#215).
  * Fix: support chunked-prefill prompts in the disaggregated serving
    handoff, with a 1M-token admission cap and pool release on extract
    error (#213).
  * Fix: apply the chat stream filter to disaggregated router output (#212);
    finish a chunked prefill when the first chunk reaches the prompt end
    (#179).
  * Fix: release paged KV block pins on prompt-cache evict or decline,
    closing a pre-existing leak of the origin allocation (#170); account
    real paged pool bytes in the cache ledger and stats (#231).
  * Fix: enforce the pack3 size contracts in release builds (#236).
  * Fix: render assistant tool_calls.arguments as a JSON object on
    multi-turn (#210); render the request's tools into the prompt (#207);
    expand bare model names in the download subcommand (#177).
  * Security: harden the paged KV handoff deserialization boundary (frame
    cap, geometry anchor, per-layer consistency, empty-sequence rejection),
    and release already-taken blocks on a partial restore failure (#186).
  * Docs: new docs/CONTINUOUS_BATCHING.md and an expanded unified-cache
    section in docs/turbo-kv-cache.md (#194).
  * Chore: record upstream attribution for ported third-party code (#238);
    bump the minor-and-patch dependency group with 3 updates (#180).

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 13 Jun 2026 18:00:00 +0900

mlxcel (0.1.4-1~jammy1) jammy; urgency=medium

  * Added: Gemma 4 Unified (`gemma4_unified`) multimodal architecture
    (#153, closes #151).
  * Added: Gemma 4 Unified MTP speculative drafter
    (`gemma4_unified_assistant`). The Gemma 4 Unified decode target routes
    through the existing MTP burst dispatch, reusing the drafter and round
    loop unchanged. The 4-bit assistant's pre/post projections load through
    the quantization-aware UnifiedLinear, so a 4-bit assistant no longer
    crashes at forward time with a matmul shape mismatch. Temperature-0
    output is byte-identical to classic decode at about 1.87x decode
    speedup (39 to 74 tok/s) on the gemma-4-12b-it-4bit + 4-bit assistant
    pair (#157, closes #158).
  * Added (opt-in): variable-length prompts in B>1 batched MTP bursts,
    behind MLXCEL_ENABLE_MTP_BATCH_RAGGED. Rows of different prompt lengths
    join one burst via per-row left-padding; greedy parity holds because
    every token in a row is shifted by the same constant. Off by default
    (measured 0.94x to 1.13x on the 31B), so the production path is
    byte-for-byte unchanged (#162, closes #161).
  * Added: unified paged KV cache foundation (epic #116): the decode-time
    page-gather microbench and ADR 0001 (#145), physical block-pool K/V
    tensor storage in PagedBlockPool (#148), the pooled paged-decode read
    path over real block tables (#149), and the paged prefill writer with
    shared-prefix copy-on-write (#150). Additive machinery exercised by
    tests; the live decode path is byte-for-byte unchanged until the
    scheduler wiring lands.
  * Changed: B=1 MTP speculative decoding now runs by default for every
    MTP target, including batch-capable ones such as Gemma 4 31B.
    Previously batch-capable targets declined singleton MTP unless
    MLXCEL_ENABLE_MTP_B1=1 was set. M5 Max measurement shows it is
    profitable with byte-identical output at temperature 0: about 1.2x to
    1.4x on the 31B plus bf16 assistant and about 1.87x on the 12B Unified
    pair. Opt out with MLXCEL_ENABLE_MTP_B1=0 (#159, closes #158).
  * Fixed: quantized fused MoE experts in the gemma4_unified loader are now
    split correctly. The split in sanitize_gemma4_unified_weights only
    matched the bare non-quantized .weight, so a quantized MoE checkpoint's
    .weight / .scales / .biases legs fell through unsplit and switch_glu
    construction could not find its per-projection quantized parts. The
    split now matches each quantized component leg and slices it on the
    output axis at the same half boundary (#156).
  * Docs: recorded the measured Gemma 4 31B B>1 batched MTP numbers and
    aligned the related code comments (#160).
  * Chore: bumped the minor-and-patch dependency group: uuid 1.23.1 to
    1.23.2 and hyper 1.9.0 to 1.10.1 (#147).

 -- Jeongkyu Shin <inureyes@gmail.com>  Fri, 05 Jun 2026 12:30:00 +0900

mlxcel (0.1.3-1~jammy1) jammy; urgency=medium

  * Changed (BREAKING): `mlxcel list` now lists local downloaded models by
    default; the supported-architecture catalog moved to the new `mlxcel arch`
    verb (alias `mlxcel supported`), and the `mlxcel list --local` flag was
    removed (clap rejects it). Migration: use `mlxcel arch` for the catalog and
    drop `--local`, since the bare `mlxcel list` now does the same thing (#138).
  * Added: `mlxcel list` output redesign. The default table is now NAME / SIZE /
    MODIFIED (a relative time from the snapshot directory mtime); the absolute
    path moved behind `-v` / `--verbose`. New `--json` (stable array), `-q` /
    `--quiet` (repo-ids only), and `--sort name|size|modified` modes; the header
    contracts `$HOME` to `~` and dims secondary columns on a TTY, respecting
    `NO_COLOR` (#141).
  * Fixed (security): chat-template rendering is bounded with minijinja fuel (a
    50M-instruction cap) so a pathological model template can no longer cause a
    denial of service. Exhaustion returns a clean `OutOfFuel` error instead of
    panicking, and real templates run far under the cap (#129, PR #139).
  * Fixed: the base-model warning no longer presents `-it` as a universal
    instruction-tuned suffix; it now names the per-family conventions (Gemma
    `-it`; Llama and Qwen2.5 `-Instruct`; Qwen3 and Qwen3.5 plain name vs.
    `-Base`) (#137).

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 30 May 2026 16:30:00 +0900

mlxcel (0.1.2-1~jammy1) jammy; urgency=medium

  * Fix: when `tokenizer_config.json` ships no `chat_template` field
    and there is no `chat_template.jinja`, the chat fallback path no
    longer collapses into echo loops on base / non-instruction-tuned
    models. Previously `render_prompt` called `concat_plaintext`,
    which is bare content-only concatenation with no role markers,
    so a base completion model took the most natural continuation
    of an unstructured prompt and parroted the user's last turn
    indefinitely. The implicit "no template found" path now uses a
    generic `User: ... Assistant: ...` pseudo-template via
    `concat_userassistant_fallback`, with a trailing `Assistant:`
    cue (no newline) that nudges the model to produce an assistant
    turn next instead of completing its own prompt with another
    `User:` line. The `processor.is_none()` warning still fires and
    still recommends the `-it` Hub counterpart; the explicit
    `--no-chat-template` flag is unchanged and remains the offline
    `mlxcel generate --no-chat-template` parallel for raw,
    completion-style concatenation. Template-render failure inside
    the chat-template path now falls back to the structured form
    as well, rather than raw concat. Unknown roles such as `tool`
    are preserved verbatim with the same `Role: ` pattern instead
    of silently merging into the prior turn (#133, PR #136)

 -- Jeongkyu Shin <inureyes@gmail.com>  Fri, 29 May 2026 00:15:00 +0900

mlxcel (0.1.1-1~jammy1) jammy; urgency=medium

  * Fix: add `*.jinja` to the downloader file allow-list so
    `chat_template.jinja` is downloaded alongside the rest of the
    model snapshot. Previously the allow-list only accepted exact-name
    `chat_template` (no extension) plus `*.json`, `*.safetensors`,
    `*.tiktoken`, `*.model`, and constrained `*.txt`, so models that
    ship the chat template as a separate Jinja file (e.g.
    `mlx-community/gemma-4-e4b-it-4bit`) silently fell back to the
    raw-text path in the REPL. The `is_safe_relative_path` and
    `is_explicitly_denied` guards still run before the allow-list so
    no new attack surface is opened (#132, PR #134)
  * Fix: expand the `mlxcel run` warning emitted when a model ships no
    chat template. It now states that the model is likely a base /
    non-instruction-tuned variant, that chat replies will be
    incoherent or repetitive, suggests trying an `-it` variant on the
    Hub (e.g. `gemma-4-e4b-it-4bit` for `gemma-4-e4b-4bit`), and
    documents `--no-chat-template` for silent raw-text mode and
    `mlxcel generate -p <prompt>` for one-shot completion. The
    explicit `--no-chat-template` path remains completely silent
    (#132, PR #134)
  * Docs: refresh GB10 (NVIDIA Grace Blackwell) results to the
    2026-05-28 full sweep on mlxcel 0.1.0 with MLX pin `84961223`
    and the warm same-process harness (`--cooldown 0`). Adds the
    recovered `internvl3-1b` and `molmo-7b` text rows and three VLM
    image-path entries (`qwen2-vl-2b`, `qwen2-vl-2b-4bit`,
    `qwen3-vl-30b-a3b`). Aligns the cross-hardware decode table in
    `model_tests.md` so all three columns reflect the canonical
    per-hardware doc state: GB10 2026-05-28, M1 Ultra 2026-05-28,
    M5 Max 2026-05-27. Drops the "vs 2026-05-19" delta framing in
    favor of a current-state snapshot and collapses the
    `Partial (⚠️)` status into `Pass (✅)` (#131)
  * CI: notarize macOS release binaries by submitting both signed
    `mlxcel` and `mlxcel-server` to Apple's notary service via
    `rcodesign notary-submit --wait`, so Gatekeeper no longer blocks
    first launch with "developer cannot be verified". Pairs
    notarization with `rcodesign verify` after signing,
    `set -euo pipefail` on the prepare-cert and code-sign steps,
    surfaced `openssl pkcs12` stderr on extraction failure, up-front
    validation of `APPLE_CERTIFICATE`, `APPLE_CERTIFICATE_PASSWORD`,
    and `AC_API_*` secrets, `chmod 600` on the materialized PEM and
    API key files, and an always-run cleanup that scrubs
    `signing.pem`, `original.p12`, `AuthKey.p8`, `ac-key.json`, and
    the notarization zip from `$RUNNER_TEMP` so self-hosted runners
    cannot carry an unencrypted Developer ID private key across jobs
  * CI: add a per-target `workflow_dispatch` filter (`targets`:
    `all` / `macos` / `linux`) to the release workflow.
    Re-uploading a single platform's artifact to an existing release
    no longer rebuilds and replaces the other platforms' bit-different
    (timestamp-driven) zips. Release events still build everything;
    the filter is dispatch-only
  * CI: pin `actions/checkout` ref to the target release tag in both
    the macOS and Linux CUDA jobs (release event:
    `github.event.release.tag_name`; `workflow_dispatch`:
    `github.event.inputs.release_tag`; otherwise `github.sha`).
    Without an explicit ref, `actions/checkout` would grab the
    dispatched ref (which is `main` for `workflow_dispatch`), so
    re-dispatching a build for an older tag would silently use
    `main` HEAD's source instead of the tag's source. The workflow
    YAML itself still runs from the dispatched ref, matching
    `all-smi`'s self-healing release pattern

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 28 May 2026 22:31:44 +0900

mlxcel (0.1.0-1~jammy1) jammy; urgency=medium

  * Added: `mlxcel run <repo-id-or-path>` subcommand. No `-p` enters
    the interactive chat REPL; with `-p` it produces one-shot output
    matching `mlxcel generate`. No model argument falls back to
    `mlx-community/Llama-3.2-3B-Instruct-4bit`, matching mlx-lm's
    DEFAULT_MODEL (#102)
  * Added: interactive multi-turn chat REPL. `mlxcel generate` without
    `-p` now streams a chat session token-by-token, preserves
    conversation context across turns, and supports `/bye`, `/clear`,
    and `/?` slash commands plus `"""` multiline blocks (#101)
  * Added: local model management. `mlxcel list --local` lists
    downloaded snapshots with on-disk size and absolute path;
    `mlxcel rm <repo-id>` deletes from the mlxcel store and reports
    freed bytes, prompts on TTY, requires `--yes` on non-TTY, and
    refuses to touch the read-only HuggingFace cache (#99)
  * Added: repo-id-aware `-m/--model` across `generate`, `serve`,
    `inspect`, `mlxcel-server`, and `run`. An `owner/name` repo-id is
    resolved against legacy `./models/<name>`, HF cache,
    mlxcel store, and then auto-downloaded into the store. Existing
    local paths are still used as-is (#100, #92)
  * Added: global model store at
    `${MLXCEL_CACHE_DIR:-$HOME/.cache/mlxcel}/models/<owner>/<name>`,
    shared across working directories. `download_repo` reuses an
    existing HF Hub snapshot read-only when no `--local-dir` is
    pinned, and never writes into the HF content-addressed layout
    (#98)
  * Added: `MLXCEL_MODELS_DIR` environment variable and a uniform
    `--models-dir` flag for `download`, `generate`, `serve`,
    `inspect`, `run`, `list`, and `rm`, with the precedence
    `--models-dir` > `MLXCEL_MODELS_DIR` >
    `${MLXCEL_CACHE_DIR:-$HOME/.cache/mlxcel}/models` (#108)
  * Added: bare model names (no `/`) resolve as
    `mlx-community/<name>` across every `-m` consumer. The default
    org is overridable via `MLXCEL_DEFAULT_ORG`; existing paths and
    explicit `owner/name` inputs are unchanged (#113)
  * Fix: harden the `mlxcel-server` legacy startup path so `-m` uses
    the same repo-id resolver and `--models-dir` store override as
    `mlxcel serve`, including the safetensors-only presence check
    (#92)
  * Fix: thread video resource caps (`MLXCEL_VIDEO_MAX_PIXELS` /
    `_DURATION_SEC` / `_PNG_FRAME_BYTES`) through a `VideoLimits`
    struct resolved once at the boundary instead of reading
    `std::env` deep in the decode path. Removes a libc setenv/getenv
    data race that made the per-cap tests flake under the threaded
    test runner (#104)
  * Fix: bind `127.0.0.1:0` directly through `TcpTransport` in the
    `distributed::pipeline::runtime` tests, removing the
    release-then-rebind window that caused "stub stage startup
    channel dropped" failures when another test grabbed the freed
    ephemeral port (#106)
  * Fix: acquire `env_lock()` in
    `require_secure_endpoint_refuses_plaintext_with_token` so it no
    longer races with sibling tests setting
    `MLXCEL_ALLOW_INSECURE_ENDPOINT` (#111)
  * Docs: lead the README "Run a model" section with the
    `mlxcel run` one-liner, collapse the verb examples, and move the
    store-root precedence and env-var detail to
    `docs/environment-variables.md`. README drops about 58 lines
    with no loss of documented behavior (#114)
  * Docs: refresh the M5 Max detailed table, README headline
    version, and benchmark report method table to the 2026-05-27
    full-sweep state on mlxcel 0.1.0. `internvl3-1b` passes in both
    text and VLM (#115)
  * Docs: recompute M5 Max decode aggregates against the unchanged
    `mlx-lm` / `mlx-vlm` baselines. Text average 98% to 99% (median
    99%, 62 of 66 at >=90% parity); VLM average 101% to 102%,
    median 100% to 101%, 22 comparable pairs (was 20), 18 of 22 at
    >=90% parity. README decode tables add Gemma 2 2B, Phi-3.5-mini,
    Jamba, and InternVL3 1B (#127)
  * Docs: reflect the 2026-05-28 M1 Ultra full sweep across README,
    benchmark report, and `model_tests_m1ultra.md`. M1 Ultra text
    99% median (64 of 74 at >=90%); VLM 98% median (12 of 18 at
    >=90%); `internvl3-1b` now passes on M1 (#130)
  * Docs: reword the v0.0.31 #86 entry to match the GitHub release
    note: the real bug was a batch-scheduler concurrency race where
    a burst of concurrent VLM requests overwrote a shared
    `per_layer_inputs` cell, not just a per-layer input shape issue

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 28 May 2026 14:18:39 +0900

mlxcel (0.0.31-1~jammy1) jammy; urgency=medium

  * Added: MiniCPM-V 4.6 VLM architecture, including hardened image
    grid handling (#82, #83)
  * Added: RT-DETRv2 object detection model with the new mlxcel detect
    subcommand (#80)
  * Added: Anthropic-style /v1/messages API endpoint on the server
    (#74)
  * Fix: tolerate missing or null chat message content, such as
    assistant tool-call turns, instead of rejecting it with HTTP 422
    (#91)
  * Fix: key the Gemma 3n VLM per_layer_inputs state per sequence id so
    concurrent VLM requests in the batch scheduler no longer race on a
    single shared cell and read the wrong sequence's tensor (#86)
  * Fix: use per-position verify attention for Qwen3.5 MTP speculative
    decoding so draft and verify stay in parity (#78)
  * Fix: correct the mask offset for batched quantized KV caches (#76)
  * Docs: document MLXCEL_CAPTURE_DECODE and clarify the memory
    headroom wording (#72)
  * CI: pin the Rust toolchain to 1.93.1 for reproducible builds
    (#87, #90)
  * Chore: bump serde_json to 1.0.150 and minijinja to 2.20.0 (#84)

 -- Jeongkyu Shin <inureyes@gmail.com>  Wed, 27 May 2026 01:32:48 +0900

mlxcel (0.0.30-1~jammy1) jammy; urgency=medium

  * Added: unified pre-load memory estimator. The new mlxcel inspect
    subcommand prints a byte-level breakdown of weights, KV cache, and
    runtime headroom against available unified memory without loading
    any tensors. --estimate-memory on generate and serve runs the same
    estimator as a preflight that aborts when the model will not fit,
    with --force to override and MLXCEL_MEMORY_LIMIT to set a soft cap
    (#64, #65, #66, #67)
  * Added: Molmo v1 (molmo-7b) VLM architecture (#41)
  * Added: InternVL (internvl_chat) VLM architecture (#37)
  * Perf: reduce Gemma 3n bf16 decode AltUp/MLP graph overhead and
    improve M5 decode bandwidth with pretransposed weights (#60, #62)
  * Perf: speed up Phi-3.5 SuScaledRoPE decode, align Gemma dense GeGLU
    with mlx-lm, and speed up Jamba hybrid decode (#42, #43, #44)
  * Changed: treat the server --ctx-size as a total context budget shared
    across request slots, matching llama.cpp semantics (#57)
  * Fix: validate CLI boolean cache flags and let CLI flags take
    precedence over environment variables (#70)
  * Fix: make the prompt cache radix trie iterative to prevent a stack
    overflow on deep prompts (#63)
  * Fix: gate the Gemma 3n bf16 fused decode path off the M5 Neural
    Accelerator (#61)
  * Fix: append the 90a arch suffix for CUDA Hopper auto-detect and
    fallback builds (#51)
  * Fix: harden VLM server image decoding to skip invalid entries (#50)
  * Fix: expand the Qwen2-VL image placeholder to the grid count (#39)
  * Docs: refresh M1 Ultra and README decode benchmarks and keep the
    result tables current-state only (#45, #46, #47, #48, #49)

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 23 May 2026 10:28:35 +0900

mlxcel (0.0.29-1~jammy1) jammy; urgency=medium

  * Fix: model-owned VLM fallback state reset. Single-row CLI and
    benchmark generations, which do not carry a SequenceId, reused stale
    fallback caches between runs. A new
    LanguageModel::reset_runtime_state() hook, invoked from
    CxxGenerator::reset_with_model, clears the model-owned fallback slot
    in lockstep with the generator-owned cache vector for Gemma 3,
    Gemma 4, Llama 4, and Qwen 3.5 Next (#34)
  * Fix: Gemma 3n VLM padded prefill alignment. Gemma 3n VLM prefill
    aborted with a [broadcast_shapes] mismatch when the projected
    per-layer-inputs tensor diverged from the tile-padded token stream.
    The per-layer tensor is now aligned to the embeddings sequence length
    (pad when shorter, slice when longer) before the per-layer blend (#36)
  * Added: mlxcel-bench-decode same-process benchmark harness that loads a
    model once and runs the warmup and measured passes in one process,
    plus the matching bench_mlxlm.py mlx-lm / mlx-vlm baseline sweep (#36)
  * Docs: reorganize benchmark result reports, refresh Apple Silicon and
    M5 Max VLM benchmarks, and clarify README benchmark phases and
    quickstart (#32, #33)

 -- Jeongkyu Shin <inureyes@gmail.com>  Wed, 20 May 2026 04:00:00 +0900

mlxcel (0.0.28-1~jammy1) jammy; urgency=medium

  * Perf: Gemma3n bf16 prefill path: materialize full-precision bf16
    casts and preserve the Gemma3n language MLP bf16 path; decode on
    gemma3n-e4b-bf16 improves from 11.16 to 38.81 tok/s on M5 Max
    while output stays coherent (#28)
  * Perf: Qwen3-VL text-only decode: fuse Q/K/V into a single
    FusedQKVLinear and add a forward_text_only fast path on Attention,
    DecoderLayer, and the top-level model that skips MRoPE position-id
    computation and visual-state propagation when no image is present.
    Qwen3-VL-30B-A3B-Instruct-4bit decode 56.00 to 146.29 tok/s
    (2.61x, 99% of mlx-lm parity); Qwen3-VL-32B-Instruct-4bit decode
    18.79 to 27.33 tok/s (1.45x, 94% of mlx-lm parity). Image-in-prompt
    and DeepStack paths are unchanged (#29)
  * Perf: GatedDeltaNet fast RMSNorm: replace the expanded square /
    mean_axis / sqrt / divide / multiply Q/K and gated-output RMSNorm
    graphs in Qwen3.5, Qwen3-Next, and KimiLinear with mlx::fast::rms_norm
    kernel calls via a new shared scaled_fast_rms_norm_no_weight helper.
    qwen3.5-0.8b-4bit decode 425.16 to 535.43 tok/s on M5 Max (+25.9%,
    96% of mlx-lm parity); Qwen3.5 speculative-decoding verify pass is
    updated alongside prefill / decode so draft / verify dtype agreement
    is preserved (#30)
  * Fix: Rewrite mlxcel list output to drive from the ModelType
    registry, fixing stale count, missing VLM family, missing ~30 model
    types, and removing a broken docs link (#27)
  * Docs: refresh README performance snapshot and docs/benchmarks.md to
    match the latest M5 Max sweep

 -- Jeongkyu Shin <inureyes@gmail.com>  Tue, 19 May 2026 04:00:00 +0900

mlxcel (0.0.27-1~jammy1) jammy; urgency=medium

  * Feat: Speculative decoding Phase 2 — end-to-end Gemma 4 MTP and
    Qwen 3.5 DFlash drafter pipelines. Drafter trait + DrafterKind
    enum + model_type auto-detection; MaskedEmbedder for E2B/E4B
    drafters; drafter masks (bidirectional full + SWA) +
    normalize_batched_shared_kv_states; Gemma4AssistantDraftModel
    4-layer drafter + pre/post projections; DFlashDraftModel
    5-layer drafter + DFlashAttention + DFlashKVCache;
    Gemma 4 return_hidden / return_shared_kv / rollback_speculative_cache
    target hooks; Qwen 3.5 return_hidden + capture_layer_ids +
    GDN-aware rollback_speculative_cache
  * Feat: Speculative round loops — DFlash single-batch round loop,
    MtpGenerator single-batch round loop, batched
    DFlash round loop with continuous batching + GDN-aware rollback,
    batched MTP round loop with continuous batching +
    left-padding normalization
  * Feat: CLI — --draft-kind {dflash,mtp} and --draft-block-size
    flags; test(speculative) greedy-parity + perf benchmarks
    for drafter pairings
  * Feat: Server speculative dispatch — wire speculative dispatch
    resolution and MtpTarget adapters; wire real MaskedEmbedder
    and make_drafter_masks for Gemma 4 assistant; wire speculative
    dispatch into scheduler via B=1 bursts; B>1 batched
    speculative dispatch via MtpBatchedGenerator / DFlashBatchedGenerator;
    cancellation propagation through MtpGenerator and
    DFlashGenerator; thread token_history through speculative-burst
    first sample; logprobs support in speculative-burst;
    thinking-budget enforcement in speculative-burst; wire
    donate_finished_sequence_cache symmetric with classic path
    and into the B>1 batched arm; real-model byte-equality
    end-to-end tests; harden speculative drafter epic follow-ups
  * Fix: DFlash drafter lazy-bind for upstream z-lab/Qwen3.5-4B-DFlash
    checkpoint; enable DFlash for Qwen 3.5 VLM text requests;
    avoid slow Gemma 4 MTP singleton bursts; validate
    trimmable cache and reserve last token in prefill for speculative
    rollback safety
  * Perf: Qwen 3.5 DFlash decode-path greedy argmax optimization
  * Feat: OpenAI Responses API (Phase 1) — /v1/responses endpoint
    with conversation store, streaming events, reasoning-trace
    forwarding, response cancellation, and four new CLI flags on both
    binaries
  * Feat: APC block-level partial cache adoption in the scheduler —
    when APC is on, a prompt sharing the first N blocks with a cached
    entry but diverging at block N+1 now reuses blocks 0..N and
    re-prefills only from the divergence boundary via
    DetachedKVCache::trim_to / DetachedCacheSet::truncate_to (FP16,
    INT8, Turbo4, Turbo4Delegated sidecars). APC-off retains bit-exact
    prior behaviour
  * Feat: Nemotron H Nano Omni audio modality — Parakeet/Conformer
    sound encoder, mel-spectrogram feature extractor, audio projector,
    interleaved audio+vision token merging at sound_context_token_id
    in generate_vlm
  * Feat: mlxcel download / mlxcel-server download per-file +
    aggregate progress bars with terminal-aware suppression, reqwest
    streaming, and atomic tempfile rename; follow-up
    security hardening — plaintext HF_ENDPOINT + token warning
    (M1), client connect/read timeouts (M2), URL path percent-encoding
    for ?/#/% in filenames (L1), O_NOFOLLOW tempfile creation (L2),
    Result-based token ASCII handling (L3), stale .mlxcel-partial.*
    cleanup at start (L5), parallel HEAD with buffer_unordered(8)
    (L6)
  * Feat: Server --max-kv-size flag (matching llama-server) and
    tighter chat-completion response shape
  * Feat: Tokenizer multi-token think and tool-call sequence support

  * Fix: MiniMax M2 parallel tool calling parser; preserve
    token positions when buffering parallel tool calls; skip
    tool→normal transition when tool_call_end is empty;
    pop_prefixes in RadixTrie with correct immediate-prefix semantics
    for the prompt cache
  * Fix: Close TOCTOU race in video allowlist by passing fd to
    ffmpeg; Gemma 4 skip k_proj/v_proj/k_norm load for
    KV-shared layers; Nemotron H default time_step_limit to
    (0.0, inf) regardless of time_step_min/max; gated_delta
    zero-init y[dv_idx] when mask is false in masked Metal kernel
    variants; upstream-sync review follow-ups
  * Chore: clippy 1.93 — replace .map_or(false, ...) with
    .is_some_and(...) in tokenizer; test fixture adds
    max_kv_size field to ServeArgs
  * CI: bump GitHub Actions to Node 24 runtime
  * Docs: refresh README to match current code and benchmarks;
    update speculative decoding guidance

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 16 May 2026 18:00:00 +0900

mlxcel (0.0.26-1~jammy1) jammy; urgency=medium

  * Feat: TurboQuant KV cache — Walsh-Hadamard transform op + PolarQuant
    Lloyd-Max codebook generator porting, KVCacheMode::Turbo4
    symmetric, Turbo4Asym Fp16-K + Turbo4-V, Turbo3Asym
    Fp16-K + Turbo3-V, Turbo4Delegated FP16 hot tail + packed
    turbo cold body, TurboQuant + RotatingKVCache sliding window
    B9, sparse-V dequant scaffolding, Boundary-V layer protection,
    packed-aware PagedKvLayout
  * Feat: --cache-type-k / --cache-type-v server flag parity with
    llama-server for TurboQuant modes
  * Feat: KV cache quantization for continuous batching; unified
    TurboQuant KV-cache CLI flags across mlxcel binaries;
    compressed dequant-SDPA paths
  * Perf: Fused Sparse-V Metal kernel skipping per-thread SDPA;
    precomputed Sparse-V kernel rescale dropping per-token threadgroup
    barriers; cold-V dequant cache + Metal kernel for
    Turbo4Delegated; unified K storage dropping per-step K
    concat; steel-attention-envelope fused SDPA kernel;
    parallelized Pass 1 softmax in turbo4_delegated_steel_sdpa;
    delegated FP16 predecode compaction + lazy sidecars
  * Test: TurboQuant wikitext-2 PPL + NIAH quality gate; VLM B3 quality gates (PPL + NIAH + image-token kurtosis) for;
    KV speed gate matrix on M1 Ultra and M5 Max
  * Feat: Automatic Prefix Caching (APC) with hash blocks
  * Feat: OpenAI-compatible response_format json_schema constrained
    decoding via llguidance with schema size/depth/$ref limits
  * Feat: mlxcel download / mlxcel-server download HuggingFace
    subcommand with allow-list filter, atomic writes, and path-traversal
    defense
  * Feat: /health endpoint exposes context_size and tool_call_parser

  * Feat: Paged scheduler dispatch on PagedKvLayout::cache_mode;
    thread-local generation stream and uniform-batch RoPE collapse
  * Feat: Gemma 4 video support and VLM video input infrastructure;
    Youtu-VL model; Nemotron H Nano Omni vision;
    video_url content blocks wired through chat completion handler;
    single-pass ffmpeg frame extraction with Drop guard
  * Fix: Per-sequence MRoPE alignment for mixed VL+text batches;
    per-sequence per_layer_inputs for Gemma 4 E2B/E4B VLM;
    mixed-length batching for Gemma 4; relaxed cached-position
    shape check in Qwen VL chunked prefill; Qwen3.5-MoE batch
    size validation on cached position_ids reuse
  * Fix: Streamed detokenization for byte-fallback tokens; top_p
    filter for batched logits; token queue timeout during long
    prefills; tool-call markup stripped from streamed
    delta.content covering Hermes <tool_call> and Mistral Nemo
    [TOOL_CALLS]
  * Fix: TurboQuant batch cache offset merging; Turbo3 split-flag
    + ENV_LOCK race
  * Fix: Gemma3-4B attention SIGABRT — sliding-window mask T_k mismatch;
    Qwen2 fused QKV bias preservation;
    nemotron-h-nano-omni post-merge validation gaps
  * Update: MLX upstream pin bumped twice — first to v0.32.0
    (c9aa5605), then to 84961223 covering CUDA QMM kernel split,
    JIT preamble rename, and AsStrided contiguity-flag fix (PRs #3443
    #3463 #3475)
  * Docs: TurboQuant KV cache user guide and validated config matrix;
    mlx-lm version reference 0.31.2 → 0.31.3; M1 Ultra
    benchmark refresh to 2026-05-08 and benchmarks-by-hardware.md
    full M1 Ultra column resync

 -- Jeongkyu Shin <inureyes@gmail.com>  Sun, 10 May 2026 18:00:00 +0900

mlxcel (0.0.25-1~jammy1) jammy; urgency=medium

  * Feat: Cross-sequence prompt-prefix KV cache — KVCache trim/detach/adopt
    API, paged-cache block-table prefix reuse, PromptCacheStore shared LRU,
    longest common token-prefix matcher, scheduler integration that adopts
    cached prefix and prefills only the suffix
  * Feat: Multimodal/vision-aware prompt cache key (MultimodalDigest) and
    prefix stability hardening with session-key parsing
  * Feat: OpenAI-compatible cached_tokens reporting + Prometheus metrics for
    prompt cache; multi-turn E2E test and prefill-latency benchmark
  * Feat: --prompt-cache-size, --prompt-cache-min-tokens CLI flags and
    matching LLAMA_ARG_* env vars wired through mlxcel-server
  * Feat: Language-bias steering Axis B Phase 1 — lang_analyzer Unicode
    script classifier, TokenLanguageIndex builder with disk cache,
    TokenBiasMap + apply_token_bias primitive, LangBiasSet Conservative/
    Strict policy, CLI flags + YAML config + LLAMA_ARG_LANG_BIAS
  * Feat: Lang-bias generator-pipeline wiring, B9 tracing fields and
    Prometheus counters, byte-fragment CJK classification via UTF-8
    start-byte analysis, byte-level reverse-map token decode
  * Feat: thinking_token_budget for Qwen3 family
  * Feat: preserve_thinking for Qwen 3.6 multi-turn reasoning retention
  * Feat: StreamFilter recognizes Qwen-style <think>/</think> for streaming
    reasoning_content
  * Feat: Extend thinking_budget_tokens to Gemma 4
  * Feat: Bridge-overhead microbench tool (Rust cxx vs Python nanobind)
  * Feat: Multi-stage pipeline-parallel CI smoke with Qwen3-0.6B fixture
  * Perf: Gemma 4 mlx-lm decode parity pass on 26B / 31B / e2b — fused
    Router RMS norm with top-k-then-softmax, fused SwitchGeGLU compile
    window, Metal-trace-driven attention/RoPE/per-layer fusion, BF16 decode
    graph alignment, proportional RoPE, SwitchGLU projection order, QKV
    projection shape, router top-k, single-query causal mask skip,
    redundant residual copy removal
  * Update: MLX upstream pin upgraded to v0.31.2; SDPA / steel-attention
    overlays dropped (now covered upstream)
  * Fix: CUDA QMM patches updated for new upstream lhs/rhs_indices
    signatures
  * Fix: Qwen 3.5 SSM decode masks aligned with mlx-lm
  * Fix: Gemma 4 enable_thinking degeneracy + reasoning_content streaming
  * Fix: emit content=null for tool-only assistant turns (OpenAI schema
    compliance)
  * Fix: chat-template flattened extra_body and pseudo-user tool responses
  * Fix: Prompt cache prefix isolation across detach/adopt boundary
  * Fix: Pipeline Parallel CI on Ubuntu (LAPACK + clippy -D warnings)
  * Fix: Gemma 4 vision encoder hidden_size read after input_proj
  * Fix: bump cc to 1.2.60 to silence BSD ar probe warning
  * Chore: SIGTERM running mlxcel-server after binary copy so respawn
    picks up the new binary

 -- Jeongkyu Shin <inureyes@gmail.com>  Fri, 24 Apr 2026 18:00:00 +0900

mlxcel (0.0.24-1~jammy1) jammy; urgency=medium

  * Feat: Zero-config multi-machine pipeline-parallel bring-up via
    --pp-auto N with --cluster-peers seeds or opt-in mDNS discovery
  * Feat: RDMA-aware transport backend with transparent TCP fallback
    (io_uring on Linux, kqueue batched send on macOS)
  * Feat: Compose pipeline parallelism with tensor parallelism as a 2D
    (pp_stage, tp_rank) mesh for Llama-70B-class topologies
  * Feat: Byte-accurate pipeline auto-partition with adjacency
    constraints for MoE and Gemma 4 KV-shared pairs (drops manual
    --pp-layers for MoE and gemma-4-e2b-it-4bit)
  * Feat: Elastic pipeline-parallel repartitioning behind
    --enable-elastic-pp (drain/rebalance/resume without cluster restart)
  * Feat: LoRA adapter composition across pipeline stages via existing
    --adapter flag (Llama family; parity with single-process run)
  * Feat: Pipeline stage-executor coverage for Mistral, Mixtral,
    DeepSeek V3, Llama 4, Jamba, Nemotron-H
  * Feat: Pipeline /metrics endpoint (per-stage utilization, bubble
    ratio, transfer latency, KV admission rejections), --metrics-port,
    --debug-pp-trace chrome-tracing output, and Grafana dashboard
  * Feat: Multi-host pipeline-parallel regression CI harness
    (2-host logical + 3-host real-model gated by ci:pp-three-host)
  * Feat: VisionFeatureCache LRU for multi-turn VLM image reuse
    (--vision-cache-size N, default 20) on Gemma 4, Qwen2.5-VL, Qwen3-VL
  * Feat: Batch scheduler null/empty-cache safety guards for empty
    prompt, filter-to-empty, and merge-of-all-empty transitions
  * Fix: Auto-detect per-layer quantization bit overrides in
    UnifiedLinear loader (enables qwen3.6-35b-a3b-4bit)
  * Fix: Use additive f32 attention mask in
    prepare_inputs_for_multimodal (prevents padding leakage through
    scaled_dot_product_attention)
  * Fix: Apply embed_scale conditionally in the tensor-parallel Gemma 4
    runtime, matching the single-GPU model
  * Fix: Wrap conv_state slice in contiguous() across mamba / mamba2 /
    nemotron-h / jamba / qwen3.5 / kimi-linear / qwen3-next to prevent
    per-step graph retention and memory growth
  * Fix (BREAKING): Apply RMS norm before projection in Gemma 4
    multimodal embedder (re-download Gemma 4 VLM checkpoints)
  * Fix: Apply sqrt(hidden_size) embed_scale to text embeddings before
    merging vision/audio features in Gemma 4 VLM
  * Fix: Implement proportional RoPE for Gemma 4 full-attention layers
    (rope_type="proportional" normalizes exponents by full head_dim)
  * Fix: Gemma 4 audio feature extractor — periodic Hann window and
    semicausal left-pad (restores correct 100-frame count for 1s audio)
  * Fix: Default NemotronH time_step_limit to (0.0, +inf) unconditionally
    when absent, matching upstream mlx-lm
  * Refactor: Replace Gemma 4 ScaledLinear with UnifiedLinear + external
    per_layer_projection_scale (bit-identical math)

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 18 Apr 2026 18:00:00 +0900

mlxcel (0.0.23-1~jammy1) jammy; urgency=medium

  * Fix: Render chat templates that use Python-style dict/string methods
    (.get, .items, .keys, .values, .strip, .lstrip, .rstrip, .startswith,
    .endswith, .split, .rsplit, .replace, .join, .upper, .lower, .title,
    .capitalize, .casefold, .swapcase, .find, .count, .is{digit,alpha,
    alnum,space,upper,lower}) via extended minijinja unknown-method callback
  * Fix: Pass tools as empty iterable so `tools is iterable and | length > 0`
    guards work under minijinja (Qwen 3 Next, Nemotron-H/NAS)
  * Fix: Strip HuggingFace transformers `{% generation %}`/`{% endgeneration %}`
    extension markers during template preprocessing (SmolLM 3)
  * Fix: Resolve Gemma 4 "Assistant:" echo loop on gemma-4-e4b-it-4bit caused
    by chat template silently falling back to `User: ... Assistant:` format
  * Fix: Apply Gemma 4 structural-token stream filter and non-streaming
    cleanup unconditionally, not only when tool parsing is enabled
  * Test: Add cross-model chat template render audit covering 85 locally
    available models across simple / system+user / multi-turn-with-think
    scenarios (249/249 scenarios pass)
  * Chore: Clean up pre-existing clippy warnings in distributed config, TP
    llama_runtime, thunderbolt_transport, qwen3_5 linear attention, and
    NVFP4 sanitize
  * Chore: Clean up webpage ESLint/TypeScript errors in motion-wrapper,
    downloads, footer, and use-os hook (no production impact)

 -- Jeongkyu Shin <inureyes@gmail.com>  Wed, 15 Apr 2026 18:00:00 +0900

mlxcel (0.0.22-1~jammy1) jammy; urgency=medium

  * Feat: Pipeline stage executor framework with per-family executors
  * Feat: Stage executors for Gemma 3, Gemma 4, Qwen3, Qwen3.5, GLM4, GLM MoE DSA, gpt-oss
  * Feat: Generic Llama-family pipeline stage executor seam
  * Feat: In-process pipeline stage worker loop
  * Feat: CLI pipeline generate path
  * Feat: Server pipeline runtime integration
  * Feat: Pipeline transport lifecycle controls
  * Feat: TCP-backed remote pipeline stages
  * Feat: Thunderbolt transport backend for remote pipeline parallelism
  * Feat: Multi-machine validation for remote pipeline parallelism
  * Feat: bench_decode --cooldown / --big-cooldown for M5 Max thermal management
  * Refactor: Generalize stage executor backends
  * Refactor: Transport-capable pipeline runtime seam
  * Fix: Tolerate stale model.safetensors.index.json in mlx-community repackaged quants
  * Fix: Tolerate partial text_config (no num_hidden_layers) in single-rank TP planning
  * Fix: Prevent Gemma 4 special tokens from leaking into streaming content deltas
  * Fix: Complete remote pipeline lifecycle recovery
  * Fix: bench_decode single-model runs no longer truncate the full-suite CSV
  * Fix: Log lazy pipeline peer reconnects
  * Test: Pipeline server smoke validation and pipeline rollout real-model coverage
  * Docs: M5 Max benchmark refresh for 2026-04-13 (97 models, 88 pass; 8 multimodal models restored)
  * Docs: Remote pipeline usage examples and rollout workflow

 -- Jeongkyu Shin <inureyes@gmail.com>  Mon, 13 Apr 2026 15:30:00 +0900

mlxcel (0.0.21-1~jammy1) jammy; urgency=medium

  * Feat: Paged KV cache substrate with batch scheduler integration
  * Feat: Native paged decode kernel paths for rotating and chunked caches
  * Feat: Paged compatibility for windowed caches
  * Feat: Default paged decode for supported server workers
  * Feat: Paged KV transfer observability
  * Feat: NVFP4 load-time dequantization for Gemma 4 nvfp4 checkpoints
  * Feat: F8_E4M3/F8_E5M2 safetensors loading for nvfp4 checkpoints
  * Refactor: Unify model-owned sequence state with backend seam
  * Refactor: Vectorize batched decode positional metadata
  * Fix: Skip Teams notification when webhook URL secret is not configured
  * CI: Auto-promote pre-release to full release after successful builds
  * Docs: Paged decode rollout benchmark matrix and eligibility tracking

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 12 Apr 2026 12:00:00 +0900

mlxcel (0.0.20-1~jammy1) jammy; urgency=medium

  * Feat: In-process tensor parallel runtime for Llama, Qwen2/3/3.5, Gemma 3/4, ERNIE, Hunyuan
  * Feat: Server batching support for tensor parallel runtimes
  * Feat: Gemma 4 tensor-parallel support
  * Fix: Qwen 3.5 tensor-parallel parity on large CUDA models
  * Test: Expand tp4 parity coverage for Qwen3, ERNIE, Hunyuan, Gemma 3

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 10 Apr 2026 18:00:00 +0900

mlxcel (0.0.19-1~jammy1) jammy; urgency=medium

  * Feat: Improve sharded/multi-file safetensors loading robustness
  * Fix: Ensure input contiguity in QuantizedMatmul for MLA models on CUDA
  * Fix: Skip models exceeding system memory in bench script
  * Fix: Increase CUDA warmup timeout and add JIT preheat to bench script
  * CI: Add Teams release notification via Power Automate webhook

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 10 Apr 2026 12:00:00 +0900

mlxcel (0.0.18-1~jammy1) jammy; urgency=medium

  * Update: Upgrade MLX upstream to b98831ad with GatherQMM CUDA and SM80/naive QMM
  * Fix: Add mixed-type bf16/float overloads to CUDA binary_ops for Gemma 4 and Qwen 3.5
  * Fix: Remove stale NO_GPU(BlockMaskedMM) override conflicting with upstream
  * Perf: GB10 CUDA 14 models recovered, 24 models 10-180% faster

 -- Jeongkyu Shin <inureyes@gmail.com>  Tue, 08 Apr 2026 12:00:00 +0900

mlxcel (0.0.17-1~jammy1) jammy; urgency=medium

  * Fix: Resolve broadcast crash in Gemma 4 chunked prefill with undersized attention mask

 -- Jeongkyu Shin <inureyes@gmail.com>  Sun, 06 Apr 2026 12:00:00 +0900

mlxcel (0.0.16-1~jammy1) jammy; urgency=medium

  * Feat: Add audio input support for server chat completions endpoint
  * Feat: Implement Gemma 4 audio encoder and audio-language model support
  * Feat: Start Metal 4 fused attention path
  * Feat: Implement OpenAI-compatible tool calling support
  * Feat: Add M5 GPU acceleration experiments
  * Feat: Continue M5 Neural Accelerator rollout research
  * Update: Unify attention dispatch for Metal 4 path
  * Fix: Propagate client disconnection to BatchScheduler
  * Fix: Harden tool calling with input limits and parser improvements
  * Fix: Remove eval() calls from qwen3_moe forward hot path
  * Fix: Resolve Gemma SDPA crash on M1 for head_dim=256
  * Fix: Update compiled.cpp patch for upstream MLX API change
  * Fix: Add str.split() support in chat template for Gemma 4 multi-turn

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 05 Apr 2026 18:00:00 +0900

mlxcel (0.0.15-1~jammy1) jammy; urgency=medium

  * Feat: Add Gemma 4 text and VLM model support
  * Feat: Add user-facing warning when loading full-precision bf16 models
  * Fix: Extend bf16-to-f16 weight conversion to all Apple Silicon generations
  * Perf: Audit f32 upcasts and optimize MoE gate sigmoid for fp16 co-issue
  * Update: Improve Metal 4 fused attention scaffolding with research docs
  * Chore: Reuse cached MLX source for faster rebuilds
  * Feat: Add download webpage with Next.js static site (EN/KO)

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 03 Apr 2026 18:00:00 +0900

mlxcel (0.0.14-1~jammy1) jammy; urgency=medium

  * Feat: Add logprobs support for chat completions and completions endpoints
  * Feat: Add runtime Apple Silicon generation detection
  * Feat: Add prefill tile alignment for M5 Neural Accelerator
  * Feat: Add batched speculative decode verification for NA utilization
  * Feat: Add batched prefill in server mode
  * Feat: Add layer pipelining with strategic async_eval
  * Feat: Add Metal 4 fused attention kernel scaffolding
  * Feat: Add KV cache INT8 quantization for memory savings
  * Feat: Add multimodal chat template support for VLM image token placement
  * Perf: Centralize bf16-to-f16 weight conversion in VLM common path
  * Perf: Skip bf16-to-f16 conversion for quantized models (+20% throughput)
  * Perf: Add compiled gelu_topk kernel
  * Perf: Expand QKV projection fusion to GQA models
  * Perf: Fuse Q/K/V projections in Gemma v1 attention
  * Fix: Auto-convert bf16 weights to f16 on M5 for Metal JIT compatibility
  * Fix: Skip add_special_tokens when prompt already contains BOS token
  * Fix: Prevent NemotronH GPU hang and state corruption on M5 Max
  * Fix: PhiMoE expert activation from GeGLU to SwiGLU
  * Fix: Replace gelu_approx power(x,3) with erf-based GELU in vision encoder
  * Fix: Patch MLX Metal kernels for macOS 26.4 compatibility

 -- Jeongkyu Shin <inureyes@gmail.com>  Thu, 03 Apr 2026 12:00:00 +0900

mlxcel (0.0.13-1~jammy1) jammy; urgency=medium

  * Feat: Add Mistral4 MLA language model support
  * Feat: Add Molmo-Point VLM model support
  * Feat: Add NemotronSuper model support (upstream mlx-lm sync)
  * Perf: Fuse GatedDeltaNet decode step with mlx::core::compile
  * Perf: Optimize Qwen3-VL MRoPE and position ID handling
  * Perf: Vectorize Qwen3-VL interleaved MRoPE with take_along_axis
  * Perf: Optimize VLM vision encoding and sampling pipeline
  * Perf: Use SDPA for NemotronH attention (+59% decode throughput)
  * Fix: SSM/Mamba2 numerical precision with float32 dt computation
  * Fix: GatedDelta numerical precision with float32 state
  * Fix: Mamba/NemotronNAS output corruption with softplus overflow
  * Fix: Qwen3.5 GatedDeltaNet state batch dimension mismatches
  * Fix: Ministral3 attn_scale shape reference
  * Fix: Llama4 BatchKVCache scalar offset invariant

 -- Jeongkyu Shin <inureyes@gmail.com>  Mon, 31 Mar 2026 12:00:00 +0900

mlxcel (0.0.12-1~jammy1) jammy; urgency=medium

  * Perf: Compiled C++ ops (SDPA, MLP, clip_residual) via mlx::core::compile
  * Perf: Gemma3 fused SDPA, pre-computed norms, skip decode masks (+35% for 1B)
  * Perf: GQA-aware softcap SDPA for Gemma2
  * Perf: Hoist env var checks and incremental token history in decode loop
  * Perf: MLX native load_safetensors and batched sync for model loading
  * Feat: Distributed inference framework (TP, PP, disaggregated serving)
  * Feat: Comprehensive mkdocs documentation site (EN/KO) with PDF export
  * Fix: OpenAI API streaming response format compatibility
  * Fix: Guard compiled paths against non-standard quantization params

 -- Jeongkyu Shin <inureyes@gmail.com>  Wed, 26 Mar 2026 12:00:00 +0900

mlxcel (0.0.11-1~jammy1) jammy; urgency=medium

  * Feat: Add compiled kernel fusion for relu_squared, silu, MoE gate, and compute_dt
  * Feat: Add fused SSM Metal kernel for Mamba2 single-token decode
  * Feat: Add fused MoE forward and compiled MoE gate for NemotronH
  * Feat: Add fused Mamba2 mixer forward for NemotronH
  * Feat: Add MLXCEL_PROFILE_PIPELINE and MLXCEL_FORCE_SYNC debug flags
  * Fix: Auto-cast SDPA mask to Q dtype preventing mask type errors
  * Fix: Load float16 weights natively on Metal (was converting to float32)
  * Fix: Eliminate float32 type promotion across all models
  * Fix: Prevent float32 type promotion in NemotronH hidden states
  * Fix: Add affine fast-path for quantized_matmul
  * Update: Optimize Mamba single-token decode path

 -- Jeongkyu Shin <inureyes@gmail.com>  Wed, 18 Mar 2026 12:00:00 +0900

mlxcel (0.0.10-1~jammy1) jammy; urgency=medium

  * Fix: ExaOne4 causal mask dtype mismatch with bfloat16 model weights
  * Fix: StableLM EOS token now read from config.json instead of hardcoded 0
  * Update: Add static mode string pool for quantized ops hot path

 -- Jeongkyu Shin <inureyes@gmail.com>  Tue, 17 Mar 2026 21:00:00 +0900

mlxcel (0.0.9-1~jammy1) jammy; urgency=medium

  * Feat: Add GptOss MoE model with sinks SDPA support
  * Feat: Add MXFP4/NVFP4/MXFP8 quantization mode support across FFI bridge and model layers
  * Fix: Set wired memory limit to gpu_max_memory_size by default
  * Update: Add GPT-OSS benchmark results and re-benchmark all models

 -- Jeongkyu Shin <inureyes@gmail.com>  Mon, 17 Mar 2026 18:00:00 +0900

mlxcel (0.0.8-1~jammy1) jammy; urgency=medium

  * Fix: Support explicit head_dim in Qwen VL/MoE configs (Qwen3-VL-32B)
  * Fix: Switch macOS CI runner to macos-15 for Xcode 16+ C++20 ranges support
  * Update: Add CUDA release pipeline and refresh benchmark report with MoE results

 -- Jeongkyu Shin <inureyes@gmail.com>  Mon, 17 Mar 2026 12:00:00 +0900

mlxcel (0.0.7-1~jammy1) jammy; urgency=medium

  * Feat: GatherMM/GatherQMM for MoE model support on CUDA
  * Feat: CUDA bf16 support with mixed-precision binary kernels and validation
  * Feat: Add --batch-size and --ubatch-size as llama-server compatible aliases
  * Feat: Debian packaging, man pages, and optimized release profile
  * Feat: GB10 vs M1 Ultra benchmark comparison and CUDA build guide
  * Fix: CUDA qmv shared-mem optimization with block.sync() fix
  * Fix: C++ bridge build fixes (LTO, C++20)
  * Update: Bump MLX to v0.31.1, show GPU backend in runtime display

 -- Jeongkyu Shin <inureyes@gmail.com>  Mon, 16 Mar 2026 12:00:00 +0900

mlxcel (0.0.6-1~jammy1) jammy; urgency=medium

  * Feat: Implement explicit forward_batched() for Qwen3 with split-attention
  * Feat: Continuous batching implementation with scheduler
  * Docs: Add continuous batching implementation summary report
  * Docs: Continuous batching development guide and benchmark comparison

 -- Jeongkyu Shin <inureyes@gmail.com>  Sat, 14 Mar 2026 12:00:00 +0900
